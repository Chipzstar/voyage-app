import React from 'react';
import PropTypes from 'prop-types';

const CalendarFilter = ({ current, onSelect }) => {
	return <div></div>;
};

CalendarFilter.propTypes = {
	
};

export default CalendarFilter;
